# アノテーター修正フロー（AI自動修正）

サイトアノテーター（修正ツール）に投稿された修正指示を、AIが自動で読み取り、サイトを修正・デプロイする仕組みです。ポリッシュは修正結果を確認し、AIが対応できなかった部分を手動で補完します。

**管理画面**: [https://www.propagate-annotator.com/admin](https://www.propagate-annotator.com/admin)

---

# 対応フロー

1. 顧客がサイトアノテーター（修正ツール）に修正指示を投稿する
2. M列が「デモサイト自動修正中」に自動遷移する
3. AIがアノテーター上の修正指示を読み取り、サイトを自動修正してデプロイする
4. M列が「デモサイト修正中」に自動遷移し、ポリッシュのキューに表示される
5. ポリッシュが修正結果を確認する
    - アノテーター上で「未対応」「対応中」になっている修正指示について、AIが正しく修正できているかを確認する
    - 問題なければ、該当の修正指示を「修正完了」に変更する
    - AIの修正に問題がある場合は、手動で修正し手動で「修正完了」に変更する

> ⚠️ **M列変更のタイミング・操作者**: 顧客が修正ツールに修正指示を投稿した時点で、M列は「デモサイト自動修正中」に自動遷移します。ポリッシュ/ディレクターが手動で変更する必要はありません。
> 

---

# 対応範囲と制約

- **画像の差し替え**: AIによる自動修正に対応済み（2026-04-23〜）。顧客から送られた画像は `public/uploaded/{record_id}/` に自動保存される
- **メール・LINEの確認、完了の送信**: ポリッシュが対応する
- **スマホ最適フェーズ**: Bot自動修正の対象外（実機確認が必要なため）。従来通り手動対応を継続する

---

# 修正結果の確認方法

自動修正の結果は、**アノテーターで修正前** / **ローカル開発環境で修正後** を比較して確認します。

## 確認手順

### 1. アノテーターで修正前を表示

1. **アノテーター管理画面**（[https://www.propagate-annotator.com/admin](https://www.propagate-annotator.com/admin)）を開く
2. 検索バーに案件名を入力 → 該当案件をクリック
3. 上部メニューバーの **「修正確認」** をクリック
4. 中央に表示される **スナップショット画像（修正前）** で、修正指示が指している箇所を確認

> 💡 左上の **「ライブサイトを表示」** をクリックすると、左にライブサイト（現在）／右にスナップショット（修正前）の並列表示も可能です。

### 2. ローカル開発環境で修正後を表示

ローカルで該当リポジトリを最新化（`git pull`）し、`npm run dev` で起動 → 修正後の状態を確認します。

### 3. なぜこの構成なのか

AI自動修正が入ると：

- **Gitリポジトリ**: 最新コード（AI修正後）が push 済み
- **アノテーター**: 修正指示と「修正前のスナップショット画像」が保持されている
- **Vercel ライブサイト**: 常に最新デプロイ（AI修正後）を表示する

従って、**アノテーターのスナップショットで修正前**、**ローカル開発環境（`npm run dev`）で修正後**、を見比べるのが基本フローです。

## 修正指示ごとの処理フロー

修正指示（①②③...）それぞれについて、以下を実行します。

1. **アノテーター（修正前）** と **ローカル（修正後）** を見比べ、意図通り修正されているかを確認
2. **問題なし** → アノテーター右側コメント一覧の該当コメントの **「未対応」ボタンをクリック** → 一覧から **「修正完了」** を選択
3. **問題あり** → ローカルで手動修正 → push → デプロイ反映を確認 → 「**修正完了**」に変更

---

# フィードバック

AI自動修正に関する質問、バグ報告、改善要望は、Slack チャンネル **#制作-ai_質問の場** に投稿してください。投稿時は対象のレコード番号（制作進行シートA列）とパートナー名（C列）を添えると調査がスムーズです。
