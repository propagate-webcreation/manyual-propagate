# アノテーター修正フロー（AI自動修正）

サイトアノテーター（修正ツール）に投稿された修正指示を、AIが自動で読み取り、サイトを修正・デプロイする仕組みです。ポリッシュは修正結果を確認し、AIが対応できなかった部分を手動で補完します。

**管理画面**: [https://www.propagate-annotator.com/admin](https://www.propagate-annotator.com/admin)

---

# 対応フロー

1. 顧客がサイトアノテーター（修正ツール）に修正指示を投稿する
2. M列が「デモサイト自動修正中」に自動遷移する
3. AIがアノテーター上の修正指示を読み取り、サイトを自動修正してデプロイする
4. M列が「デモサイト修正中」に自動遷移し、ポリッシュのキューに表示される
5. ポリッシュが修正結果を確認する
    - アノテーター上で「未対応」「対応中」になっている修正指示について、AIが正しく修正できているかを確認する
    - 問題なければ、該当の修正指示を「修正完了」に変更する
    - AIの修正に問題がある場合は、手動で修正し手動で「修正完了」に変更する

> ⚠️ **M列変更のタイミング・操作者**: 顧客が修正ツールに修正指示を投稿した時点で、M列は「デモサイト自動修正中」に自動遷移します。ポリッシュ/ディレクターが手動で変更する必要はありません。
> 

---

# 対応範囲と制約

- **画像の変更・差し替え**: AIによる自動修正は未対応。画像に関する修正指示はポリッシュが手動で対応する
- **メール・LINEの確認、完了の送信**: ポリッシュが対応する
- **スマホ最適フェーズ**: Bot自動修正の対象外（実機確認が必要なため）。従来通り手動対応を継続する

---

# 修正ツール — 新旧比較の確認方法

Botの自動修正結果を確認する際は、修正ツールの「修正確認」画面を使う。修正前のスナップショットと現在のライブサイトを左右に並べて比較できる。

## 修正確認画面の開き方

1. 修正ツール（[https://www.propagate-annotator.com/admin](https://www.propagate-annotator.com/admin)）で対象サイトを開く
2. 上部メニューバーの**「修正確認」**ボタンをクリックする

## 画面の見方

修正確認画面では、修正前後の状態を左右に並べて比較できる。

| 左側 | 右側 |
| --- | --- |
| **スナップショット（修正前）** | **ライブサイト（現在）** |
| AI修正が入る前の状態 | AI修正後の現在の状態 |
- 赤枠の番号マーカー（①②③...）は修正指示の位置を示す
- 左右を見比べて、各修正指示が正しく反映されているかを確認する

## 確認の進め方

1. 左右を見比べながら、修正指示ごとに変更が正しいかを確認する
2. 問題なければ → アノテーター上で該当の修正指示を**「修正完了」**に変更する
3. 問題があれば → 該当部分を手動で修正し、手動で**「修正完了」**に変更する

---

# フィードバック

AI自動修正に関する質問、バグ報告、改善要望は、Slack チャンネル **#制作-ai_自動修正fb** に投稿してください。投稿時は対象のレコード番号（制作進行シートA列）とパートナー名（C列）を添えると調査がスムーズです。

---

# 画面キャプチャ（参考）

下記は、「Annotator管理画面」（修正確認ボタンの位置）と「新旧比較画面」（スナップショット vs ライブサイト）の実例です。

![image.png](image.png)

![image.png](image%201.png)